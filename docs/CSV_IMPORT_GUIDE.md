# CSV 导入指南

## 目录

1. [概述](#概述)
2. [Twitter CSV 导入](#twitter-csv-导入)
3. [YouTube CSV 导入](#youtube-csv-导入)
4. [评分系统说明](#评分系统说明)
5. [常见问题](#常见问题)

---

## 概述

Viralfy 支持从 Twitter Analytics 和 YouTube Studio 导出 CSV 数据,自动分析并识别高潜力内容创意。

### 为什么使用 CSV 导入?

✅ **成本为零** - 无需付费 API,数据来自官方平台
✅ **数据完整** - 包含完整的互动指标和表现数据
✅ **批量处理** - 一次性分析大量内容,快速建立创意库
✅ **已验证** - 基于真实表现数据,而非猜测

### 工作流程

```
1. 从平台导出 CSV → 2. 运行导入命令 → 3. 自动分析评分 → 4. 保存高潜力创意
```

---

## Twitter CSV 导入

### 第一步:导出 Twitter Analytics 数据

#### 方法 1: 通过 Twitter Analytics (推荐)

1. 访问 [Twitter Analytics](https://analytics.twitter.com)
2. 登录您的 Twitter 账号
3. 点击顶部导航栏的 **"Tweets"** 标签
4. 选择日期范围(建议最近 90 天)
5. 点击右上角的 **"Export data"** 按钮
6. 等待 CSV 文件下载完成

#### 方法 2: 通过 Twitter 数据归档

1. 访问 Twitter 设置 → Your Account → Download an archive
2. 等待 Twitter 准备数据(可能需要 24 小时)
3. 下载并解压归档文件
4. 找到 `tweets.csv` 文件

### 第二步:理解 CSV 格式

**Twitter Analytics CSV 包含的关键字段**:

| 字段名 | 说明 | 用途 |
|--------|------|------|
| Tweet id | 推文 ID | 唯一标识 |
| Tweet text | 推文内容 | 提取为创意标题 |
| time | 发布时间 | 时间分析 |
| impressions | 展示量 | 计算传播速度 |
| engagements | 互动总数 | 计算互动率 |
| engagement rate | 互动率 | 受众质量指标 |
| retweets | 转发数 | 传播指标 |
| replies | 回复数 | 参与度指标 |
| likes | 点赞数 | 互动指标 |

**CSV 示例**:

```csv
Tweet id,Tweet text,time,impressions,engagements,engagement rate,retweets,replies,likes
1234567890,"如何用 AI 10 倍提升开发效率",2025-01-15 10:30,15000,1200,0.08,180,45,975
1234567891,"个人品牌打造的 7 个关键步骤",2025-01-14 14:20,22000,2100,0.095,320,88,1692
```

### 第三步:运行导入命令

```bash
# 在 Viralfy 项目目录中运行
viralfy validate

# 选择 "2. 导入 Twitter CSV"
# 输入 CSV 文件路径,例如:
# ~/Downloads/tweet_activity_metrics.csv
```

### 导入过程

脚本将自动:

1. **验证格式** - 检查 CSV 是否包含必需的列
2. **计算基准** - 计算所有推文的平均展示量和互动数
3. **识别高表现** - 筛选出互动数或展示量 > 3x 平均值的推文
4. **计算评分** - 使用多维度算法计算 0-100 分
5. **保存创意** - 将高潜力创意保存到 `ideas/validated-topics.json`

### 评分标准

高表现推文的识别标准:
- 互动数 > 平均值的 3 倍,**或**
- 展示量 > 平均值的 3 倍

---

## YouTube CSV 导入

### 第一步:导出 YouTube Studio 数据

1. 访问 [YouTube Studio](https://studio.youtube.com)
2. 点击左侧菜单的 **"Analytics"**(分析)
3. 点击右上角的 **"Advanced mode"**(高级模式)
4. 选择 **"Content"**(内容)标签
5. 点击右上角的 **"Export"**(导出)按钮
6. 选择 CSV 格式
7. 选择日期范围(建议最近 90 天或全部时间)
8. 下载 CSV 文件

### 第二步:理解 CSV 格式

**YouTube Studio CSV 包含的关键字段**:

| 字段名 | 说明 | 用途 |
|--------|------|------|
| Video title | 视频标题 | 提取为创意标题 |
| Video ID | 视频 ID | 唯一标识 |
| Views | 观看量 | 传播指标 |
| Watch time (hours) | 观看时长 | 内容质量 |
| Average view duration | 平均观看时长 | 留存率 |
| Impressions | 展示量 | 曝光量 |
| Click-through rate (%) | 点击率 | 吸引力 |
| Likes | 点赞数 | 互动指标 |
| Comments | 评论数 | 参与度 |
| Shares | 分享数 | 病毒性 |

**CSV 示例**:

```csv
Video title,Video ID,Views,Watch time (hours),Likes,Comments,Shares
AI 内容创作完整指南,abc123xyz,150000,5000,8500,450,320
个人品牌 0 到 1 实战教程,def456uvw,220000,7200,12000,680,580
```

### 第三步:运行导入命令

```bash
# 在 Viralfy 项目目录中运行
viralfy validate

# 选择 "3. 导入 YouTube CSV"
# 输入 CSV 文件路径,例如:
# ~/Downloads/youtube-analytics-2025.csv
```

### 导入过程

脚本将自动:

1. **验证格式** - 检查 CSV 是否包含必需的列
2. **计算基准** - 计算所有视频的平均观看量和点赞数
3. **识别高表现** - 筛选出观看量 > 100k 或 > 3x 平均值的视频
4. **计算指标** - 计算点赞率、评论参与度等
5. **计算评分** - 使用多维度算法计算 0-100 分
6. **保存创意** - 将高潜力创意保存到 `ideas/validated-topics.json`

### 评分标准

高表现视频的识别标准:
- 观看量 > 100,000,**或**
- 观看量 > 平均值的 3 倍,**或**
- 点赞数 > 平均值的 3 倍

---

## 评分系统说明

### Twitter 评分算法

**多维度加权计算** (总分 100):

| 维度 | 权重 | 计算方法 |
|------|------|----------|
| **互动数据** | 40% | (当前互动数 / 平均互动数) × 40 |
| **传播速度** | 30% | (当前展示量 / 平均展示量) × 30 |
| **受众质量** | 20% | 互动率 × 200 |
| **绝对数值** | 10% | 基于互动数的分级评分 |

**绝对数值评分**:
- 互动数 > 1000: 10 分
- 互动数 > 500: 8 分
- 互动数 > 100: 5 分
- 互动数 ≤ 100: 2 分

**评分示例**:

```
推文 A:
- 互动数: 1200 (平均: 400)
- 展示量: 15000 (平均: 5000)
- 互动率: 0.08 (8%)

计算:
- 互动维度: (1200/400) × 40 = 120 → 归一化为 40
- 传播维度: (15000/5000) × 30 = 90 → 归一化为 30
- 质量维度: 0.08 × 200 = 16
- 绝对维度: 10 (互动数 > 1000)

总分: 40 + 30 + 16 + 10 = 96 分
```

### YouTube 评分算法

**多维度加权计算** (总分 100):

| 维度 | 权重 | 计算方法 |
|------|------|----------|
| **互动数据** | 40% | (当前点赞数 / 平均点赞数) × 40 |
| **传播速度** | 30% | (当前观看量 / 平均观看量) × 30 |
| **受众质量** | 20% | 点赞率 × 2000 |
| **绝对数值** | 10% | 基于观看量的分级评分 |

**绝对数值评分**:
- 观看量 > 1M: 10 分
- 观看量 > 500k: 8 分
- 观看量 > 100k: 6 分
- 观看量 ≤ 100k: 3 分

### 评分等级

| 分数范围 | 等级 | 说明 | 建议 |
|---------|------|------|------|
| 90-100 | 🔥 极高潜力 | 病毒式爆款 | 优先深度研究 |
| 70-89 | ⭐ 高潜力 | 强互动内容 | 重点关注 |
| 50-69 | ✓ 中等潜力 | 稳定表现 | 可选研究 |
| 30-49 | ⚠️ 较低潜力 | 低于预期 | 慎重考虑 |
| 0-29 | ❌ 低潜力 | 表现不佳 | 不建议投入 |

---

## 常见问题

### Q1: CSV 文件格式不正确怎么办?

**问题**: 导入时提示 "Invalid CSV format"

**解决方案**:

1. **检查编码** - 确保 CSV 文件是 UTF-8 编码
   ```bash
   file -I your-file.csv
   # 如果不是 UTF-8,转换:
   iconv -f ISO-8859-1 -t UTF-8 input.csv > output.csv
   ```

2. **检查分隔符** - 确保使用逗号(,)而非分号(;)或制表符

3. **检查列名** - Twitter CSV 必须包含 "Tweet" 列,YouTube CSV 必须包含 "Video" 列

4. **移除空行** - CSV 文件不应包含空行

### Q2: 没有找到任何高表现内容?

**可能原因**:

1. **数据量太少** - 需要至少 20 条以上的数据才能准确计算平均值
2. **所有内容表现接近** - 没有明显的高表现内容(变异系数太小)
3. **阈值过高** - 3x 平均值对于某些账号可能过高

**解决方案**:

- 尝试导出更长时间范围的数据(90 天或更多)
- 手动输入您认为高潜力的创意
- 降低阈值(需修改脚本,将 3 改为 2)

### Q3: 如何处理非英文内容?

Viralfy 完全支持中文和其他 Unicode 字符。确保:

1. CSV 文件使用 UTF-8 编码
2. 终端支持 Unicode 显示
3. 使用支持 Unicode 的文本编辑器打开 validated-topics.json

### Q4: 可以同时导入 Twitter 和 YouTube 数据吗?

可以!多次运行导入命令,数据会累积保存到 `validated-topics.json`。

```bash
# 第一次导入 Twitter
viralfy validate
# 选择 "2. 导入 Twitter CSV"

# 第二次导入 YouTube
viralfy validate
# 选择 "3. 导入 YouTube CSV"

# 查看所有创意
viralfy validate list
```

### Q5: 导入后如何查看和管理创意?

```bash
# 查看所有已验证创意
viralfy validate list

# 查看详细的 JSON 数据
cat ideas/validated-topics.json | jq '.'

# 按评分排序(如果安装了 jq)
cat ideas/validated-topics.json | jq '.topics | sort_by(.score) | reverse'

# 导出为 Markdown 表格
cat ideas/validated-topics.json | jq -r '.topics[] | "| \(.score) | \(.title) | \(.source) |"'
```

### Q6: CSV 文件太大怎么办?

**建议**:

- 单次导入限制在 1000 行以内
- 如果文件过大,可以手动分割:
  ```bash
  # 保留表头并提取前 1000 行
  head -n 1 large-file.csv > part1.csv
  tail -n +2 large-file.csv | head -n 1000 >> part1.csv

  # 提取接下来的 1000 行
  head -n 1 large-file.csv > part2.csv
  tail -n +1002 large-file.csv | head -n 1000 >> part2.csv
  ```

### Q7: 评分不准确怎么办?

评分算法基于相对表现和统计分析,如果觉得不准确:

1. **增加数据量** - 更多数据使平均值更准确
2. **调整权重** - 修改 `validate.sh` 中的评分权重
3. **手动调整** - 直接编辑 `validated-topics.json` 修改评分
4. **结合主观判断** - 评分仅供参考,最终依靠您的专业判断

### Q8: 如何导出已验证创意供团队使用?

```bash
# 导出为易读的 Markdown
cat ideas/validated-topics.json | jq -r '
  "# 已验证创意列表\n\n| 评分 | 标题 | 来源 | 状态 |\n|------|------|------|------|\n" +
  (.topics[] | "| \(.score) | \(.title) | \(.source) | \(.status) |")
' > ideas/validated-topics.md

# 导出为 CSV
cat ideas/validated-topics.json | jq -r '
  ["score", "title", "source", "status"] as $headers |
  $headers, (.topics[] | [.score, .title, .source, .status]) |
  @csv
' > ideas/validated-topics-export.csv
```

---

## CSV 模板文件

为了帮助您理解正确的格式,我们提供了示例模板文件:

- `templates/csv-examples/twitter-sample.csv` - Twitter CSV 示例
- `templates/csv-examples/youtube-sample.csv` - YouTube CSV 示例

---

## 下一步

成功导入创意后:

1. **查看创意列表**
   ```bash
   viralfy validate list
   ```

2. **开始深度研究**
   ```bash
   viralfy research --topic topic-001
   ```

3. **创作 Newsletter**
   ```bash
   viralfy write --mode create
   ```

---

## 技术支持

如果遇到问题:

1. 查看 [GitHub Issues](https://github.com/your-org/viralfy/issues)
2. 加入社区讨论
3. 提交 Bug 报告

---

**祝您创作愉快!** 🚀
